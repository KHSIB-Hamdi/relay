# Copy to frontend/.env. CRA only exposes variables prefixed with REACT_APP_.

# Base URL of the Express backend
REACT_APP_API_URL=http://localhost:8000

# Keep ESLint warnings from failing the dev server
ESLINT_NO_DEV_ERRORS=true
